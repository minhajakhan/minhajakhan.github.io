Notes
1. I couldn't add a thumbnail preview list of all the posts in each category because doing so would require me to automate creating a new html file every time a user submitted a form to create a new post. I can automatically create a new HTML file when a user submits POST data, but it requires server-side scripting (like PHP, Python, or Node.js) because client-side JavaScript cannot directly write files to the server's file system. Given the constraints of the assignment, I was unable to add this feature. 
2. 

Fututre Features
1. Add intermediary page between Home Page and category (Electronic Music etc.) post. Envision album art in a 400 x 400 tile serving as a hyperlink to the full post, with artist name, album title, and genre underneath. 3 tiles per row. Use Node JS solution to creating a new html file automatically upon POST form submission from the user. 